@page "/"
@inject DadosCarteiras dados
<p>Data do Sistema:</p>
<br>
<p>@dados.DataDoSistema</p>

<br><br>
<p>Atualizar Data</p>

<input type="datetime" @bind=dados.DataDoSistema></input>

<br>
<button @onclick="cobrar">Cobrar Tarifas (Todas as Contas) </button>
@code{

    public void cobrar()
    {
        foreach (var carteira in dados.ListaDados)
        {
            carteira.CobrarTarifa();
            int MesCobrado = DateTime.Now.Month;

        }
        if ()
    }
}